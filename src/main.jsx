import React from 'react';
import ReactDOM from 'react-dom/client';
import MinimalGame from './MinimalGame.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <MinimalGame />
    </React.StrictMode>,
)
